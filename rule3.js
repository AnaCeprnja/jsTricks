// different terms for inputs

function compressBoxesTwice(boxes, boxes2) {
    boxes.forEach(function (boxes) {
        console.log(boxes);
    })

    boxes.forEach(function (boxes) {
        console.log(boxes);
    })
}

// first 4 loop is going to depend on how big the first item or input is
// the second for loop depends on how big the second input is --> in this case boxes2

//the big o is --> O(a+b) aka --> box one plus box two